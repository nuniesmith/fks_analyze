# Health check configuration for fks_analyze
# Use this in your Deployment spec

apiVersion: v1
kind: ConfigMap
metadata:
  name: fks_analyze-health-config
  namespace: fks-trading
data:
  liveness_probe: |
    httpGet:
      path: /health
      port: 8008
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
  
  readiness_probe: |
    httpGet:
      path: /ready
      port: 8008
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3

---
# Example Deployment with health checks
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fks_analyze
  namespace: fks-trading
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fks_analyze
  template:
    metadata:
      labels:
        app: fks_analyze
    spec:
      containers:
      - name: fks_analyze
        image: nuniesmith/fks_analyze:latest
        ports:
        - containerPort: 8008
        livenessProbe:
          httpGet:
            path: /health
            port: 8008
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8008
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
